<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="prev_enter">
	   
    <select id="pre_venterBoardSearch" parameterType="hashmap" resultType="hashmap">
    <include refid="common.pre_venter_pagingPre"/>
    <![CDATA[
             SELECT
	         IDX,
             company_name,
             area,
             n_year,
             t_year,
             biz_detail,
             sa_number,
             su_date,
             company_zip,
             company_address,
             ceo_name,
             p_number,
             c_number,
             e_mail,
             homepage,
             req_date,
             CONTENTS			 
             FROM prev_enterprise
             WHERE
              company_name like '%${company_name}%'
              AND DEL_GB = 'N'
             ORDER BY req_date DESC
			 ]]>
    <include refid="common.pre_venter_SearchpagingPost"/>
    </select>
    
    <select id="pre_venterBoardList" parameterType="hashmap" resultType="hashmap">
    <include refid="common.pre_venter_pagingPre"/>
    <![CDATA[
             SELECT
	         IDX,
             company_name,
             area,
             n_year,
             t_year,
             biz_detail,
             sa_number,
             su_date,
             company_zip,
             company_address,
             ceo_name,
             p_number,
             c_number,
             e_mail,
             homepage,
             req_date,
             CONTENTS			 
             FROM prev_enterprise
             WHERE
             DEL_GB = 'N'
             ORDER BY req_date DESC
			 ]]>
    <include refid="common.pre_venter_pagingPost"/>
    </select>
    <insert id="insertPreVenterprise" parameterType="hashmap" useGeneratedKeys="true" keyProperty="IDX">
    
        INSERT INTO prev_enterprise
        (
            company_name,
            area,
            n_year,
            t_year,
            biz_detail,
            sa_number,
            su_date,
            company_zip,
            company_address,
            ceo_name,
            p_number,
            c_number,
            e_mail,
            homepage,
            req_date,
            CONTENTS
        )
        VALUES
            ("${company_name}",
             "${area}",
             "${n_year}",
             "${t_year}",
             "${biz_detail}",
             "${sa_number}",
             "${su_date}",
             "${company_zip}",
             "${company_address}",
             "${ceo_name}",
             "${p_number}",
             "${c_number}",
             "${e_mail}",
             "${homepage}",
             NOW(),
             "${CONTENTS}")    
      </insert>
      <insert id="insertPreVenter_Mony" parameterType="hashmap" useGeneratedKeys="true" keyProperty="IDX">
    
        INSERT INTO prev_enterprise_mony
        (
            board_seq,
            years,
            ja_mony,
            me_mony,
            ji_list,
            ji_mony,            
            req_date
        )
        VALUES
            ("${idx}",
             "${years}",
             "${ja_mony}",
             "${me_mony}",
             "${ji_list}",
             "${ji_mony}",             
             NOW())    
      </insert>      
      <select id="selectPreVenterpriseDetail" parameterType="hashmap" resultType="hashmap">
    <![CDATA[
        SELECT
             IDX,
             company_name,
             area,
             n_year,
             t_year,
             biz_detail,
             sa_number,
             su_date,
             company_zip,
             company_address,
             ceo_name,
             p_number,
             c_number,
             e_mail,
             homepage,
             req_date,
             CONTENTS			 
             FROM prev_enterprise
        WHERE
            IDX = #{IDX}
        AND
            DEL_GB = 'N'       
    ]]>
      </select>
      <select id="selectPreVenterprise_Mony" parameterType="hashmap" resultType="hashmap">
    <![CDATA[
        SELECT
             IDX,
             board_seq,
             years,
             ja_mony,
             me_mony,
             ji_list,
             ji_mony,
             req_date
             FROM prev_enterprise_mony
        WHERE
            board_seq = #{IDX}
        AND
            DEL_GB = 'N'       
    ]]>
      </select>           

</mapper>